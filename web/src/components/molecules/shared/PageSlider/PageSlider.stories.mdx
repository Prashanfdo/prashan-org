import { Meta, Canvas, Story } from '@storybook/addon-docs';
import PageSlider from '.';

<Meta title="Molecules/Shared/PageSlider" />

# < PageSlider />

<Canvas>
  <Story name="Default">
    <div className="max-w-screen-lg">
      <PageSlider
        images={Array(6)
          .fill('')
          .map((_, i) => `https://picsum.photos/seed/${i}/200/300`)}
      />
    </div>
  </Story>
  <Story name="SM">
    <div className="max-w-screen-sm">
      <PageSlider
        images={Array(6)
          .fill('')
          .map((_, i) => `https://picsum.photos/seed/${i}/200/300`)}
      />
    </div>
  </Story>
  <Story name="MD">
    <div className="max-w-screen-md">
      <PageSlider
        images={Array(6)
          .fill('')
          .map((_, i) => `https://picsum.photos/seed/${i}/200/300`)}
      />
    </div>
  </Story>
</Canvas>
